const http = require("http");;
const getOrderData = require("/home/adhil/nodejs-training/adil/nodejs/orders.json");

const server =http.createServer{(req,res) => (
    try {
        
    } catch (error) {
        console.log(error);
    }
)